[project]
name = "capstone-backend"
version = "0.1.0"
description = "Flask backend for capstone project"
requires-python = ">=3.8"
dependencies = [
    "Flask==3.0.0",
    "flask-cors==4.0.0",
]

[tool.uv]
package = false
